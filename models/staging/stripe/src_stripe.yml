version: 2

sources:
  - name: jaffle_shop
    database: raw
    schema: jaffle_shop
    tables:
      - name: payment
        loaded_at_field: _etl_loaded_at
        freshness:
          warn_after: {count: 199, period: hour}
          error_after: {count: 200, period: hour}